Задание 1. Создайте программу на Python или Java, которая принимает два списка чисел и выполняет следующие действия: a. Рассчитывает среднее значение каждого списка. b. Сравнивает эти средние значения и выводит соответствующее сообщение:

""Первый список имеет большее среднее значение"", если среднее значение первого списка больше.
""Второй список имеет большее среднее значение"", если среднее значение второго списка больше.
""Средние значения равны"", если средние значения списков равны.
Важно: Приложение должно быть написано в соответствии с принципами объектно-ориентированного программирования. Используйте Pytest (для Python) или JUnit (для Java) для написания тестов, которые проверяют правильность работы программы. Тесты должны учитывать различные сценарии использования вашего приложения. 

Используйте pylint (для Python) или Checkstyle (для Java) для проверки качества кода.
Решение: Отчет pylint 
![Отчёт](Снимок5.jpg)


Сгенерируйте отчет о покрытии кода тестами. Ваша цель - достичь минимум 90% покрытия.
Решение: Отчет о покрытии тестами
![Отчёт](Снимок4.jpg)

Объяснение того, какие сценарии покрыты тестами и почему вы выбрали именно эти сценарии.
Ответ:
Данный набор тестов покрывает следующие сценарии:

1. test_calculate_average_with_empty_list: Проверяет, что функция calculate_average() возвращает 0 для пустого списка.
2. test_calculate_average_with_non_empty_list: Проверяет, что функция calculate_average() правильно вычисляет среднее значение для непустого списка.
3. test_compare_averages_with_list1_greater: Проверяет, что функция compare_averages() правильно определяет, что первый список имеет большее среднее значение, чем второй список.
4. test_compare_averages_with_list2_greater: Проверяет, что функция compare_averages() правильно определяет, что второй список имеет большее среднее значение, чем первый список.
5. test_compare_averages_with_equal_averages: Проверяет, что функция compare_averages() правильно определяет, что средние значения двух списков равны.
6. test_compare_averages_with_empty_lists: Проверяет, что функция compare_averages() правильно обрабатывает случай, когда оба списка пусты и средние значения равны.
7. test_compare_averages_with_one_empty_list: Проверяет, что функция compare_averages() правильно обрабатывает случай, когда один из списков пуст, а другой - непуст.
8. test_compare_averages_with_negative_numbers: Проверяет, что функция compare_averages() правильно обрабатывает случай, когда списки содержат отрицательные числа и первый список имеет большее среднее значение.
9. test_compare_averages_with_decimal_numbers: Проверяет, что функция compare_averages() правильно обрабатывает случай, когда списки содержат десятичные числа и первый список имеет большее среднее значение.

Выбор данных сценариев обусловлен стремлением покрыть широкий спектр возможностей функций calculate_average() и compare_averages(), чтобы обеспечить их корректное поведение в различных ситуациях, включая крайние и нетипичные случаи (пустые списки, списки с отрицательными и десятичными числами).